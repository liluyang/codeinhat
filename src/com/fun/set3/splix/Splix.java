package com.fun.set3.splix;

public class Splix {
  /**
   * With initial board setting, run a sequence of moves. If player is live,
   * return 1, if player is dead, return 0.
   * 
   * Board: if a grid is taken by player, mark as 1, otherwise 0. Moves: 0=left,
   * 1=right, 2=up, 3=down
   * 
   * Rules 1. If player runs into line he created, he crashes. 2. If player runs
   * across board border, he crashes. 3. If player makes a enclosed area, he
   * turns all grids in it to 1.
   */
  public int run(int[][] board, int[] moves) {
    return 0;
  }
}